{
  "name": "permiteq-workspace",
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": ".venv\\\\Scripts\\\\python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8001",
    "dev:full": "concurrently -k -n BACKEND,FRONTEND -c yellow.bold,cyan.bold \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "fastapi": "^0.0.8",
    "python-dateutil": "^0.0.1",
    "pytz": "^10.1.1",
    "tenacity": "^1.0.4"
  }
}
